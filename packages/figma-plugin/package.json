{
  "name": "@sigma/figma-plugin",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run build --watch",
    "build": "bun run build.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sigma/shared": "workspace:*"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
